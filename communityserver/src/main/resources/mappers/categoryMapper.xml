<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gamecommunityserver.mapper.CategoryMapper">

    <insert id="register" parameterType="com.gamecommunityserver.dto.CategoryDTO">
        INSERT INTO category (categoryId, categoryName)
        VALUES (#{categoryId}, #{categoryName})
    </insert>
    <select id = "categoryDuplicateCheck" resultType = "int">
        SELECT COUNT(*)
        FROM category
        WHERE categoryName = #{categoryName}
    </select>
    <select id="categoryIdCheck" resultType="int">
        SELECT COUNT(*)
        FROM category
        WHERE categoryId = #{categoryId}
    </select>
    <select id = "categoryDelete" resultType="int">
        DELETE FROM category
        WHERE categoryId = #{categoryId}
    </select>
</mapper>