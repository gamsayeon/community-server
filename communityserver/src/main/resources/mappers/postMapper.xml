<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gamecommunityserver.mapper.PostMapper">

    <insert id="addPost" parameterType="com.gamecommunityserver.dto.PostDTO">
        INSERT INTO post (postNumber, categoryNumber, userNumber, postName, adminPost, contents, createTime, suggestionCount, views)
        VALUES (#{postNumber}, #{categoryNumber}, #{userNumber}, #{postName}, #{adminPost}, #{contents}, #{createTime}, #{suggestionCount}, #{views})
    </insert>

    <select id="checkedAccessPost" resultType = "int">
        SELECT COUNT(*)
        FROM post
        WHERE post.userNumber = #{usernumber}
        and postNumber = #{postnumber}
    </select>

    <select id="updatePost">
        UPDATE post
        SET postName = #{postname},
        contents = #{contents}
        WHERE postNumber = #{postnumber}
    </select>

    <select id = "selectPost" resultType = "com.gamecommunityserver.dto.PostDTO">
        UPDATE post
        SET views = views + 1
        WHERE postNumber = #{postnumber}
    </select>

    <delete id = "deletePost">
        DELETE FROM POST
        WHERE postNumber = #{postnumber}
        and userNumber = #{usernumber}
    </delete>
</mapper>