<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gamecommunityserver.mapper.FileMapper">

    <insert id="addFile" parameterType="com.gamecommunityserver.dto.FileDTO">
        INSERT INTO file (fileNumber, postNumber, path, fileName, extension)
        VALUES (#{fileNumber}, #{postNumber}, #{path}, #{fileName}, #{extension})
    </insert>
</mapper>