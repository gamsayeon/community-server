<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gamecommunityserver.mapper.UserInfoMapper">

    <insert id="register" parameterType="com.gamecommunityserver.dto.UserDTO">
        INSERT INTO user (number, id, password, name, admin, createDate, userSecession)
        VALUES (#{number}, #{id}, #{password}, #{name}, #{admin}, #{createDate}, #{userSecession})
    </insert>

    <select id="idCheck" resultType="int">
        SELECT COUNT(id)
        FROM user
        WHERE id = #{id}
    </select>

    <select id="passwordCheck" resultType="com.gamecommunityserver.dto.UserDTO">
        SELECT *
        FROM user
        WHERE id = #{id} and password = #{password} and userSecession = 0
    </select>

    <select id="idNumberCheck" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE number = #{idnumber} and userSecession = 0
    </select>

    <select id="deleteUser">
        UPDATE user
        SET userSecession = 1
        WHERE number = #{idnumber}
    </select>

    <select id = "idToidnumber" resultType="int">
        SELECT number
        FROM user
        WHERE id = #{id}
    </select>
    <select id = "selectUser" resultType="String">
        SELECT id, name, admin, createDate
        FROM user
        WHERE number = #{idnumber}
    </select>

</mapper>